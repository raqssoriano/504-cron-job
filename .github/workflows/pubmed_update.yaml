
name: pubmed_update

on:
  # schedule:
  #   - cron: "0 5 * * *" # run once a day at midnight
  workflow_dispatch:  # manual triggers

jobs:
  run-data-pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo content
        uses: actions/checkout@v4

      - name: Setup python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run PubMed Retrieval Script
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
        run: python3 https://github.com/raqssoriano/504-cron-job/blob/867ae26b239bba61955023cdc8cfd8a681910811/pubmed_github_action.py # run data pipeline

     