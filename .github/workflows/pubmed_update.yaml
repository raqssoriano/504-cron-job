name: just an update

on:
  schedule:
    - cron: "* * * * *" # run every  minute
  workflow_dispatch: # manual triggers 


jobs:
  run-data-pipeline:
    runs-on: ubuntu-latest
    steps:
      # print this message
      - name: Run my REMINDER script
        run: echo "LIFE IS BEAUTIFUL!!!"

 



     